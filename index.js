/**
 * @format
 */

import {AppRegistry} from 'react-native';
import WeatherProject from './example/flashcards';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => WeatherProject);
